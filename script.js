/**
 * AI for Students — Interactive Features
 * (scripts will be added as we build each part)
 */

'use strict';

function scrollToNext() {
  const next = document.querySelector('#part-2');
  if (next) next.scrollIntoView({ behavior: 'smooth' });
}
